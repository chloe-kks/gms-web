import React, { Component } from 'react';

class Predicts extends Component {
    render() {
        return (
            <div className="Main">
                [Some graphs in machine learning (detail)]
            </div>
        );
    }
}

export default Predicts;
